# No secrets are needed for the foundation.
# Future backend credentials belong in an ignored .env, never VITE_* variables.
# Environment loading must be implemented explicitly when settings are introduced.
